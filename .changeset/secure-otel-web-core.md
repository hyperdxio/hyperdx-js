---
'@hyperdx/browser': patch
'@hyperdx/otel-web': patch
---

Align the browser OpenTelemetry dependencies with the patched 0.219.0 and 2.8.0 release lines so consumers no longer install vulnerable `@opentelemetry/core` 2.7.1 copies.
